<UserControl
    x:Class="RD3.Views.PIDView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    Width="1350"
    Height="700"
    d:DataContext="{d:DesignInstance Type=viewmodels:PIDViewModel}"
    Background="{StaticResource BackGroundBrush}"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontSize="{DynamicResource TextFontSize}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <Border Background="{StaticResource BackGroundBrush}" CornerRadius="{StaticResource DefaultCornerRadius}">
        <Grid>
            <Grid.RowDefinitions>
                <!--  操作按钮  -->
                <RowDefinition Height="70" />
                <!--  数据表格  -->
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0">
                <Button
                    Name="Save"
                    Width="80"
                    Height="40"
                    Margin="10"
                    HorizontalAlignment="Right"
                    Command="{Binding SaveCommand}"
                    FontSize="18">
                    保存
                </Button>
            </DockPanel>

            <DataGrid
                Name="dataGrid"
                Grid.Row="1"
                hc:DataGridAttach.CanUnselectAllWithBlankArea="False"
                hc:DataGridAttach.ShowSelectAllButton="False"
                AutoGenerateColumns="False"
                Background="{StaticResource DefaultBrush}"
                CanUserAddRows="True"
                CanUserSortColumns="False"
                ColumnWidth="SizeToHeader"
                GridLinesVisibility="All"
                HeadersVisibility="Column"
                HorizontalGridLinesBrush="LightGray"
                IsReadOnly="False"
                ItemsSource="{Binding PidInfos}"
                RowHeaderWidth="100"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                SelectionMode="Single"
                SelectionUnit="Cell"
                VerticalGridLinesBrush="LightGray">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding deviceID}"
                        CanUserSort="False"
                        Header="设备"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding pidName}"
                        CanUserSort="False"
                        Header="名称"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding startTime}"
                        CanUserSort="False"
                        Header="起始时间(分钟)"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="120"
                        Binding="{Binding endTime}"
                        CanUserSort="False"
                        Header="结束时间(分钟)"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding P}"
                        CanUserSort="False"
                        Header="P"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding I}"
                        CanUserSort="False"
                        Header="I"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding D}"
                        CanUserSort="False"
                        Header="D"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding deadArea}"
                        CanUserSort="False"
                        Header="死区"
                        IsReadOnly="False" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding maxSpeed}"
                        CanUserSort="False"
                        Header="上限"
                        IsReadOnly="False" />
                    <DataGridTemplateColumn
                        MinWidth="100"
                        CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                        Header="模式"
                        HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}">
                        <!--<DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <hc:UniformSpacingPanel Orientation="Horizontal" Spacing="5">
                                    <Button Content="手动" Name="btnExcuteType" Width="0" Click="btnExcuteType_Click"/>
                                </hc:UniformSpacingPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>-->
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <hc:UniformSpacingPanel Orientation="Horizontal" Spacing="5">
                                    <Button
                                        Name="btnExcuteType"
                                        Width="70"
                                        Click="btnExcuteType_Click"
                                        Content="{Binding excuteType}" />
                                </hc:UniformSpacingPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn
                        MinWidth="100"
                        CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                        Header="操作"
                        HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <hc:UniformSpacingPanel Orientation="Horizontal" Spacing="5">
                                    <Button
                                        Name="btnExcute"
                                        Width="70"
                                        Click="btn_excute_Click"
                                        Content="开始" />
                                </hc:UniformSpacingPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
