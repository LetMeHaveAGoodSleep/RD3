<UserControl
    x:Class="RD3.Views.MFCView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:MFCViewModel}"
    d:DesignHeight="50"
    d:DesignWidth="200"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FontSize="{StaticResource SubHeadFontSize}"
    mc:Ignorable="d">
    <hc:UniformSpacingPanel Orientation="Horizontal" Spacing="15">
        <DockPanel LastChildFill="False">
            <Image
                Width="29"
                DockPanel.Dock="Bottom"
                Source="/Images/NewReactor/阀（开）.png"
                Visibility="{Binding MFCInfo.IsEnable, Converter={StaticResource Boolean2VisibilityConverter}}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding MFCSettingCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Image>
            <Image
                Width="29"
                DockPanel.Dock="Bottom"
                Source="/Images/NewReactor/阀（关）.png"
                Visibility="{Binding MFCInfo.IsEnable, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
        </DockPanel>
        <TextBox
            Width="80"
            materialDesign:HintAssist.Hint="流量(L/min)"
            IsEnabled="False"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding MFCInfo.FlowRate, StringFormat={}{0:F2}}" />
        <StackPanel Orientation="Horizontal">
            <TextBlock
                Width="70"
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Text="{Binding MFCInfo.GasName}" />
        </StackPanel>
    </hc:UniformSpacingPanel>
</UserControl>
