<UserControl
    x:Class="RD3.Views.CameraImageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3.Shared;assembly=RD3.Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:pass="clr-namespace:RD3.Extensions"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:rd3="clr-namespace:RD3"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    Width="700"
    Height="607"
    d:DataContext="{d:DesignInstance Type=viewmodels:CameraImageViewModel}"
    rd3:DraggableUserControlAttach.IsDraggable="True"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontSize="{DynamicResource TextFontSize}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="Width" Value="{Binding FormWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Height" Value="{Binding FormHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Left" Value="{Binding LeftLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Top" Value="{Binding TopLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="ShowInTaskbar" Value="True" />
            <Setter Property="AllowsTransparency" Value="False" />
            <Setter Property="WindowStyle" Value="ThreeDBorderWindow" />
            <Setter Property="WindowState" Value="{Binding FormState, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Background" Value="{StaticResource BackGroundBrush}" />
            <Setter Property="Opacity" Value="1" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Border Style="{StaticResource BorderCard}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="253*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Image
                Name="image"
                Grid.Row="1"
                Margin="5,5,5,5" />
            <Button
                Name="btnDefoaming"
                Grid.Row="2"
                Width="94"
                Height="30"
                Margin="538,0,0,0"
                HorizontalAlignment="Left"
                HorizontalContentAlignment="Center"
                hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                Background="{StaticResource PrimaryBrush}"
                BorderBrush="{StaticResource PrimaryBrush}"
                Command="{Binding AFCommand}"
                Content="{ex:Language Key=手动消泡}"
                FontSize="{DynamicResource NormalFontSize}"
                Foreground="White"
                Visibility="{Binding Enable, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button
                Name="btnClose"
                Grid.Row="2"
                Width="48"
                Height="30"
                Margin="637,0,0,0"
                HorizontalAlignment="Left"
                HorizontalContentAlignment="Center"
                hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                BorderBrush="{StaticResource PrimaryBrush}"
                Command="{Binding CancelCommand}"
                Content="{ex:Language Key=关闭}"
                FontSize="{DynamicResource NormalFontSize}"
                Foreground="{StaticResource PrimaryBrush}"
                RenderTransformOrigin="1.896,0.4" />
            <Label
                x:Name="label"
                Grid.Row="2"
                Margin="344,0,0,0"
                HorizontalAlignment="Left"
                Content="消泡阈值" />
            <TextBox
                x:Name="txtDefoamingThreshold"
                Grid.Row="2"
                Width="44"
                Margin="412,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Text="{Binding CurrentDeviceParameter.DefoamingSetting.DefoamingThreshold}"
                TextWrapping="Wrap" />

            <Label
                x:Name="label2"
                Grid.Row="2"
                Margin="-3,0,0,0"
                HorizontalAlignment="Left"
                Content="消泡液流速" />
            <TextBox
                x:Name="txtDefoamingSpeed"
                Grid.Row="2"
                Width="44"
                Margin="80,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Text="{Binding CurrentDeviceParameter.DefoamingSetting.FlowSpeed}"
                TextWrapping="Wrap" />
            <Label
                x:Name="label3"
                Grid.Row="2"
                Margin="127,0,0,0"
                HorizontalAlignment="Left"
                Content="ml/min" />

            <Label
                x:Name="label4"
                Grid.Row="2"
                Margin="192,0,0,0"
                HorizontalAlignment="Left"
                Content="消泡液" />
            <TextBox
                x:Name="txtDefoamingValue"
                Grid.Row="2"
                Width="44"
                Margin="250,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Text="{Binding CurrentDeviceParameter.DefoamingSetting.FlowCapacity}"
                TextWrapping="Wrap" />
            <Label
                x:Name="label5"
                Grid.Row="2"
                Margin="297,0,0,0"
                HorizontalAlignment="Left"
                Content="ml" />
            <CheckBox
                x:Name="cbAutoDefoaming"
                Grid.Row="2"
                Margin="458,0,0,0"
                HorizontalAlignment="Left"
                Content="自动消泡"
                IsChecked="{Binding CurrentDeviceParameter.DefoamingSetting.AutoDefoaming}" />
        </Grid>
    </Border>
</UserControl>
