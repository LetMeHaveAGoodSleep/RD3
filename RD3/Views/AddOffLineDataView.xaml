<UserControl
    x:Class="RD3.Views.AddOffLineDataView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3.Shared;assembly=RD3.Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:pass="clr-namespace:RD3.Extensions"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:rd3="clr-namespace:RD3"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    Width="220"
    Height="360"
    d:DataContext="{d:DesignInstance Type=viewmodels:AddOffLineDataViewModel}"
    rd3:DraggableUserControlAttach.IsDraggable="True"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontSize="{DynamicResource TextFontSize}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="Width" Value="{Binding FormWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Height" Value="{Binding FormHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Left" Value="{Binding LeftLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Top" Value="{Binding TopLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="ShowInTaskbar" Value="False" />
            <Setter Property="AllowsTransparency" Value="True" />
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="WindowState" Value="{Binding FormState, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Opacity" Value="1" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Border Margin="0,0,0,0" Style="{StaticResource BorderCard}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>
            <hc:UniformSpacingPanel
                Grid.Row="0"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="sampleName"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="样品名称"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="{Binding OffLineSampleDatas.sampleName}" />
            </hc:UniformSpacingPanel>
            <hc:UniformSpacingPanel
                Grid.Row="1"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="batchID"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="批次"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="{Binding OffLineSampleDatas.batchID}" />
            </hc:UniformSpacingPanel>
            <hc:UniformSpacingPanel
                Grid.Row="2"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="collectTime"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="采样时间"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="{Binding OffLineSampleDatas.collectTime}" />
            </hc:UniformSpacingPanel>
            <hc:UniformSpacingPanel
                Grid.Row="3"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="collecter"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="采集人员"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="{Binding OffLineSampleDatas.collecter}" />
            </hc:UniformSpacingPanel>

            <hc:UniformSpacingPanel
                Grid.Row="4"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="O2"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="O2"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="" />
            </hc:UniformSpacingPanel>
            <hc:UniformSpacingPanel
                Grid.Row="5"
                Margin="0,0,0,0"
                Orientation="Vertical"
                Spacing="10">
                <TextBox
                    Name="CO2"
                    Width="200"
                    Margin="4,0,4,0"
                    hc:InfoElement.Title="CO2"
                    IsEnabled="True"
                    Style="{StaticResource TextBoxExtend}"
                    Text="" />
            </hc:UniformSpacingPanel>

            <StackPanel
                Grid.Row="6"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="80"
                    Height="30"
                    Margin="5"
                    HorizontalAlignment="Right"
                    HorizontalContentAlignment="Center"
                    hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    Command="{Binding CloseCommand}"
                    Content="{ex:Language Key='Cancel'}"
                    FontSize="{DynamicResource NormalFontSize}"
                    Foreground="{StaticResource PrimaryBrush}" />
                <Button
                    Width="80"
                    Height="30"
                    Margin="5"
                    HorizontalAlignment="Right"
                    HorizontalContentAlignment="Center"
                    hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                    Background="{StaticResource PrimaryBrush}"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    Click="Button_Click"
                    Content="{ex:Language Key='OK'}"
                    FontSize="{DynamicResource NormalFontSize}"
                    Foreground="White"
                    Visibility="{Binding Enable, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
