<UserControl
    x:Class="RD3.Views.FeedGradientView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Selector="clr-namespace:RD3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:ext="clr-namespace:RD3.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
    Width="1400"
    Height="880"
    d:DataContext="{d:DesignInstance Type=viewmodels:FeedGradientViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="Transparent"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <Border Background="{StaticResource BackGroundBrush}" CornerRadius="{StaticResource DefaultCornerRadius}">
        <Grid>
            <Grid.RowDefinitions>
                <!--  操作按钮  -->
                <RowDefinition Height="auto" />
                <!--  数据表格  -->
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Margin="5" LastChildFill="False">
                <hc:UniformSpacingPanel
                    DockPanel.Dock="Right"
                    Orientation="Horizontal"
                    Spacing="10">
                    <Button
                        Width="80"
                        Height="30"
                        Command="{Binding AddCommand}"
                        Content="新增数据"
                        Style="{StaticResource ButtonPrimary}" />
                    <Button
                        Name="DeleteRow"
                        Width="80"
                        Height="30"
                        Click="DeleteRow_Click"
                        Content="删除数据"
                        Style="{StaticResource ButtonPrimary}" />
                    <Button
                        Name="SaveColumn"
                        Width="80"
                        Height="30"
                        Click="SaveColumn_Click"
                        Content="保存数据"
                        Style="{StaticResource ButtonPrimary}" />
                </hc:UniformSpacingPanel>
            </DockPanel>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <DataGrid
                    Name="dataGrid"
                    Grid.Column="0"
                    hc:DataGridAttach.CanUnselectAllWithBlankArea="False"
                    hc:DataGridAttach.ShowSelectAllButton="False"
                    AutoGenerateColumns="False"
                    Background="{StaticResource DefaultBrush}"
                    CanUserAddRows="False"
                    CanUserSortColumns="False"
                    CellEditEnding="dataGrid_CellEditEnding"
                    ColumnWidth="SizeToHeader"
                    GridLinesVisibility="All"
                    HeadersVisibility="Column"
                    HorizontalGridLinesBrush="LightGray"
                    IsReadOnly="False"
                    ItemsSource="{Binding FeedGradientInfos}"
                    LayoutUpdated="DataGridMain_OnLayoutUpdated"
                    RowHeaderWidth="100"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Single"
                    SelectionUnit="Cell"
                    VerticalGridLinesBrush="LightGray">
                    <DataGrid.Columns>
                        <!--  设备列表  -->
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding BeginTime}"
                            CanUserSort="True"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="开始时间(分钟)"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding EndTime}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="结束时间(分钟)"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridComboBoxColumn
                            x:Name="colType"
                            Width="180"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="策略类型"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False"
                            SelectedValueBinding="{Binding InfoType, UpdateSourceTrigger=PropertyChanged}" />
                        <DataGridTemplateColumn
                            Width="auto"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="Stat参数"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}">
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <hc:ButtonGroup Style="{StaticResource ButtonGroupSolid}">
                                        <RadioButton
                                            Content="常量"
                                            IsChecked="{Binding IsConstant, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                        <RadioButton
                                            Content="多项式"
                                            IsChecked="{Binding IsPolynomial, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                        <RadioButton
                                            Content="指数"
                                            IsChecked="{Binding IsExp, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                    </hc:ButtonGroup>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <hc:ButtonGroup Style="{StaticResource ButtonGroupSolid}">
                                        <RadioButton
                                            Content="常量"
                                            IsChecked="{Binding IsConstant, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                        <RadioButton
                                            Content="多项式"
                                            IsChecked="{Binding IsPolynomial, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                        <RadioButton
                                            Content="指数"
                                            IsChecked="{Binding IsExp, UpdateSourceTrigger=PropertyChanged}"
                                            IsEnabled="{Binding IsEnabled}" />
                                    </hc:ButtonGroup>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding A}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="参数1"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding B}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="参数2"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding C}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="参数3"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridTextColumn
                            Width="120"
                            Binding="{Binding D}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextCenterCellStyle}"
                            Header="参数4"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="False" />
                        <DataGridTextColumn
                            Width="*"
                            Binding="{Binding Description}"
                            CanUserSort="False"
                            CellStyle="{StaticResource DataGridTextLeftCellStyle}"
                            Header="描述"
                            HeaderStyle="{StaticResource DataGridCenterColumnHeaderStyle}"
                            IsReadOnly="True" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

    </Border>
</UserControl>
