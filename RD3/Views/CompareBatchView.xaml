<UserControl
    x:Class="RD3.Views.CompareBatchView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:RD3"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    xmlns:wpf="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
    Width="auto"
    Height="auto"
    MinWidth="1200"
    MinHeight="600"
    d:DataContext="{d:DesignInstance Type=viewmodels:CompareBatchViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="1200"
    Background="{StaticResource BackGroundBrush}"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontSize="{DynamicResource TextFontSize}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <UserControl.Resources>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
    </UserControl.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <hc:ComboBox
            Name="CmbTimeInterval"
            Width="200"
            HorizontalAlignment="Left"
            hc:InfoElement.Title="时间间隔"
            BorderBrush="{DynamicResource PrimaryBrush}"
            BorderThickness="1"
            FontSize="{DynamicResource SubHeadFontSize}"
            Style="{StaticResource ComboBoxExtend}">
            <ComboBoxItem Content="时" />
            <ComboBoxItem Content="分" IsSelected="True" />
            <ComboBoxItem Content="秒" />
        </hc:ComboBox>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <wpf:WpfPlot Name="wpfPlot" Grid.Column="0" />
            <WrapPanel
                Name="flagPanel"
                Grid.Column="1"
                MouseRightButtonDown="FlagPanel_MouseRightButtonDown" />
        </Grid>
    </Grid>
</UserControl>
