<UserControl
    x:Class="RD3.Views.AddSampleLogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:RD3.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="250"
    Height="400"
    Background="{StaticResource BackGroundBrush}"
    mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="70" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBox
            Grid.ColumnSpan="2"
            Width="200"
            VerticalAlignment="Center"
            hc:InfoElement.Title="Sample ID"
            Style="{StaticResource TextBoxExtend}" />
        <TextBox
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Width="200"
            VerticalAlignment="Center"
            hc:InfoElement.Title="Batch"
            Style="{StaticResource TextBoxExtend}" />
        <ComboBox
            Grid.Row="2"
            Grid.ColumnSpan="2"
            Width="200"
            hc:InfoElement.Title="Reactor"
            ItemsSource="{Binding DataList}"
            SelectedIndex="0"
            Style="{StaticResource ComboBoxExtend}" />
        <ComboBox
            Grid.Row="3"
            Grid.ColumnSpan="2"
            Width="200"
            hc:InfoElement.Title="Type"
            ItemsSource="{Binding DataList}"
            SelectedIndex="0"
            Style="{StaticResource ComboBoxExtend}" />
        <ComboBox
            Grid.Row="4"
            Width="130"
            hc:InfoElement.Title="Main parameter"
            ItemsSource="{Binding DataList}"
            SelectedIndex="0"
            Style="{StaticResource ComboBoxExtend}" />
        <TextBox
            Grid.Row="4"
            Grid.Column="1"
            Width="70"
            VerticalAlignment="Center"
            hc:InfoElement.Title="Value"
            Style="{StaticResource TextBoxExtend}" />
        <TextBox
            Grid.Row="5"
            Grid.ColumnSpan="2"
            VerticalAlignment="Stretch"
            hc:InfoElement.Title="Remark"
            Style="{StaticResource TextBoxExtend}" />
        <StackPanel
            Grid.Row="6"
            Grid.ColumnSpan="2"
            FlowDirection="RightToLeft"
            Orientation="Horizontal">
            <Button
                Width="80"
                Height="30"
                Margin="5"
                HorizontalAlignment="Right"
                HorizontalContentAlignment="Center"
                hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                Background="{StaticResource ButtonBoderBrush}"
                BorderBrush="{StaticResource ButtonBoderBrush}"
                Content="OK"
                FontSize="14"
                Foreground="White" />
            <Button
                Width="80"
                Height="30"
                Margin="5"
                HorizontalAlignment="Right"
                HorizontalContentAlignment="Center"
                hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                BorderBrush="{StaticResource ButtonBoderBrush}"
                Content="Cancel"
                FontSize="14"
                Foreground="{StaticResource ButtonBoderBrush}" />
        </StackPanel>
    </Grid>
</UserControl>
