<UserControl
    x:Class="RD3.Views.EditSampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="clr-namespace:LayUI.Wpf.Extensions;assembly=LayUI.Wpf.Extensions"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:RD3.Shared;assembly=RD3.Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:rd3="clr-namespace:RD3"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:viewmodels="clr-namespace:RD3.ViewModels"
    Width="220"
    Height="400"
    d:DataContext="{d:DesignInstance Type=viewmodels:EditSampleViewModel}"
    rd3:DraggableUserControlAttach.IsDraggable="True"
    TextElement.FontFamily="{DynamicResource AppFontFamily}"
    TextElement.FontSize="{DynamicResource TextFontSize}"
    TextElement.FontWeight="Regular"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Ideal"
    TextOptions.TextRenderingMode="Auto"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ObjectDataProvider
            x:Key="DataFromEnum"
            MethodName="GetValues"
            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="local:SampleType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="SizeToContent" Value="WidthAndHeight" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="Width" Value="{Binding FormWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Height" Value="{Binding FormHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Left" Value="{Binding LeftLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Top" Value="{Binding TopLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="ShowInTaskbar" Value="False" />
            <Setter Property="AllowsTransparency" Value="True" />
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="WindowState" Value="{Binding FormState, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="Opacity" Value="1" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Border Style="{StaticResource BorderCard}">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="70" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBox
                Grid.ColumnSpan="2"
                Width="200"
                VerticalAlignment="Center"
                hc:InfoElement.Title="{ex:Language Key='Sample ID'}"
                IsEnabled="False"
                Style="{StaticResource TextBoxExtend}"
                Text="{Binding Sample.Id}" />
            <ComboBox
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Width="200"
                VerticalAlignment="Center"
                hc:InfoElement.Title="{ex:Language Key='Batch'}"
                ItemsSource="{Binding BatchList}"
                SelectedItem="{Binding Batch}"
                Style="{StaticResource ComboBoxExtend}" />
            <hc:CheckComboBox
                Name="cmbReactor"
                Grid.Row="2"
                Grid.ColumnSpan="2"
                MaxWidth="200"
                MaxHeight="60"
                hc:InfoElement.ShowClearButton="True"
                hc:InfoElement.Title="{ex:Language Key='Reactor'}"
                IsEnabled="{Binding Enable}"
                ItemsSource="{Binding ReactorList}"
                ShowSelectAllButton="True"
                Style="{StaticResource CheckComboBoxPlus}" />
            <ComboBox
                Grid.Row="3"
                Grid.ColumnSpan="2"
                Width="200"
                hc:InfoElement.Title="{ex:Language Key='Type'}"
                ItemsSource="{Binding Source={StaticResource DataFromEnum}}"
                SelectedItem="{Binding UserType}"
                Style="{StaticResource ComboBoxExtend}" />
            <ComboBox
                Grid.Row="4"
                Width="130"
                hc:InfoElement.Title="{ex:Language Key='Main parameter'}"
                ItemsSource="{Binding DataList}"
                SelectedIndex="0"
                Style="{StaticResource ComboBoxExtend}" />
            <TextBox
                Grid.Row="4"
                Grid.Column="1"
                Width="70"
                VerticalAlignment="Center"
                hc:InfoElement.Title="{ex:Language Key='Value'}"
                Style="{StaticResource TextBoxExtend}" />
            <TextBox
                Grid.Row="5"
                Grid.ColumnSpan="2"
                VerticalAlignment="Stretch"
                hc:InfoElement.Title="{ex:Language Key='Remark'}"
                Style="{StaticResource TextBoxExtend}" />
            <StackPanel
                Grid.Row="6"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="80"
                    Height="30"
                    Margin="5"
                    HorizontalAlignment="Right"
                    HorizontalContentAlignment="Center"
                    hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    Content="{ex:Language Key='Cancel'}"
                    FontSize="{DynamicResource NormalFontSize}"
                    Foreground="{StaticResource PrimaryBrush}" />
                <Button
                    Width="80"
                    Height="30"
                    Margin="5"
                    HorizontalAlignment="Right"
                    HorizontalContentAlignment="Center"
                    hc:BorderElement.CornerRadius="{StaticResource DefaultCornerRadius}"
                    Background="{StaticResource PrimaryBrush}"
                    BorderBrush="{StaticResource PrimaryBrush}"
                    Content="{ex:Language Key='OK'}"
                    FontSize="{DynamicResource NormalFontSize}"
                    Foreground="White"
                    Visibility="{Binding Enable, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>